# Specialist evidence and audit consistency

Three independently scoped alaa-researcher agents were dispatched: audit_backend_data (26 skills), audit_ops_frontend (26), and audit_governance (14). The lead inspected the remaining three: prompting-guide and both orchestrators. All were configured gpt-6-sol/medium; the lead request was GPT-6 Astra. No serving identity, effort or token counter was observable. The installed role declarations do not prove effective isolation; operations stayed read-only except lead-authored audit evidence in this directory.

## Additional observed native check

- Agent: audit_backend_data.
- Cwd: repository root.
- Command: `python skills/sohrab/alaa-input-normalization/assets/input-normalization/normalize_reference.py --self-test`.
- Exit: 0.
- Reported result: 145 cases times two modes; idempotence, NFC stability, length and separator-freedom properties passed.
- Command output printed abbreviated corpus digest `cd81fcdc7048ece0...`. The full digest recorded in the corpus is `cd81fcdc7048ece0c5f2253bbf9e24b8151481f18ec3913c77d7a14ef24cddaf`.
- Limitation: parent did not rerun it. This is agent-observed single-runtime evidence; no four-runtime parity or Laravel middleware integration was tested.

The lead's ten native check commands are in checks.json. Combined newly observed native checks: eleven; all returned zero at the times they ran. Historical GPT evidence is cited separately and is not a current-run pass. The final six-file source drift prevents treating affected source gates as final-tree certification.

## Deep coverage supporting decisions

Algorithms: body plus all eight references (10-complexity-budget, 20-finding-n, 30-choosing-a-structure, 40-call-in-a-loop, 50-memory-and-streaming, 60-tuning-points, 70-proving-it, 80-when-not-to) were read. No executable asset exists. None means no change to this inspected static guidance, not proven consumer performance.

Input normalization: body, all six references (10-normalization-contract, 20-browser-binding, 30-backend-middleware-binding, 40-failure-classes, 50-corpus-and-harness, 60-provider-seam-and-open-items), shell conformance entrypoint, corpus, phone fixture and Python reference were inspected. NFC maps the recorded e-plus-combining-acute case from two code points to one. The reference implementation's invariant is relative to NFC(input), unlike the problematic prose. The Laravel sample's undefined fieldName helper was compared with the official Laravel13 TransformsRequest source.

## Bounded synthesis review

The governance researcher reviewed only audit.md, per-skill.csv and versions.md against its established findings. It found no material overclaim or missing approval boundary in those artifacts. It confirmed stable/preview separation, unknown installed/consumer versions, distinct source/activation/calibration claims, Not assessed classifications, and disclosure of concurrent drift. This is a limited synthesis consistency check, not an independent complete re-audit of all 69 skills.

W0 is only an evidence dependency for affected recommendations. It grants no authority to finish, modify or take over the separate Claude migration. No recommendation is selected yet; no implementation plan, checkpoint or execution prompt exists for this program.

## Audit artifact integrity

The CSV/JSON generation asserted exactly 69 unique rows and exact set equality with inventory.json. Sizes: None1, Minor22, Fundamental1, Not assessed45. Priorities: P0=2, P1=16, P2=8, Defer=43. Priority and change size are independent. Inventory metadata never supplied a final None classification.

Only audit evidence in this directory was authored by this task. Existing source changes were preserved, no branch or commit was created, and no live benchmark, installation, publication, global configuration or consumer change was performed. Memory curation was deferred; no memory write was authorized or made.
