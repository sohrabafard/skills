# Claude Haiku 4.5

Haiku is a current comparison candidate for bounded extraction, exploration and exact
procedure execution. The policy JSON owns its exact dated API ID and capability snapshot;
it assigns no default role to Haiku without workload evidence.

Haiku uses extended thinking and has no effort parameter. Omit executable effort metadata;
an invented low-effort pin is invalid. Do not transfer adaptive-thinking API controls or
newer-generation prompting assumptions to it. Give concrete scope, required evidence,
failure reporting and stop conditions; retain the same authority and quality gates used
for other candidates.

A Haiku comparison against an effort-enabled model changes both model and control regime.
Record that limitation instead of presenting it as an isolated effort experiment. Authorize
and define that comparison separately; the bundled one-factor corpus does not run it.

Before use, verify lifecycle, provider-specific ID mapping, account access and Claude Code
support. Missing runtime evidence stays unknown; selecting a model name proves no activation.

Verified 25 September 2026 against the [model overview](https://platform.claude.com/docs/en/models/haiku-4-5/overview),
[effort reference](https://platform.claude.com/docs/en/build-with-claude/effort) and
[model lifecycle](https://platform.claude.com/docs/en/about-claude/model-deprecations).
