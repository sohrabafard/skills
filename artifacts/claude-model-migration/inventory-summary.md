# Source and installation inventory

Observed 2026-09-25 by temporary explorer (requested GPT-6 Luna/high; serving identity unknown). No installation or source change performed by that lane.

## Source boundaries

Claude orchestrator has 22 role files. Its validator independently hardcodes model/effort legality, the Sonnet high ceiling and instruction/reviewer pin parity. Grants and instruction-contract checks are separate. Claude rule-writer is under prompting-guide, outside the orchestrator roster. The complete before table is recorded in the plan, based on inspected metadata.

Affected owners: `alaa-prompting-guide` model references, source/topic maps, effort/runtime/selection/subagent guidance, rule-writer payload and grant checker; `alaa-cc-orchestrator` agents, validators, catalogs, dispatches, routing, version and changelog; shared Codex behavior only when an actual shared correction is required. `assets/codex-model-policy.json` and GPT evaluation/checker behavior are preserved.

The initial bounded inventory found no Claude manifest/build script inside the Claude orchestrator itself. Packaging discovery outside that skill remains a named implementation-lane question; absence there does not prove no repository plugin generator exists. Root indexes concern skills, not role pins. Installation docs currently copy Claude role markdown to the user agent directory; rule-writer is plugin-owned.

## Installed drift

| Surface | Observed result | Disposition |
|---|---|---|
| Codex orchestrator | installed version 3.6.0 vs source 4.0.0; GPT-5.6 installed role pins; instruction/deep profiles absent | User approved temporary GPT-6 role contracts for this task only |
| Claude skill | installed version 4.0.0; sampled body, policy/catalog/routing and three checker hashes match source | No installed changes |
| Claude user agents | 21 shipped files; hashes differ, 20 matching role model/effort pairs agree; instruction-reviewer absent | No automatic overwrite or activation claim |
| Claude plugin cache | expected user cache directory absent | Rule-writer installation comparison unavailable, not proof of absence on every plugin surface |

These are source/configuration observations, not serving identity, account eligibility or enforcement proof.

## Baseline commands

The independent baseline verifier owns current results and hashes under `baseline/`. Historical GPT logs are leads only. Root structure, index, references and lifecycle; Claude pack/grants/contracts; Codex pack; rule-writer; Codex policy and evaluation checks are in its eleven-command scope. Changed checkers require self-tests and new Claude policy/packaging checks will extend final verification.
