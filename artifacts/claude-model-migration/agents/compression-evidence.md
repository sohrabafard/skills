# Compression evidence

Draft: instruction-draft.md in this directory. Final fragments: the Claude orchestrator model-effort-policy and agent-catalog references, plus the rule-writer pin comment. The pass removed narration and repeated ownership explanations, retained canonical owner and CLI, invocation cwd, source/subset validation, exit semantics, availability limits, stable role IDs and the single-reviewer rule. No rule moved during compression. Role bodies, verdict-first metadata, independent gates and rule-writer replacement-only output are unchanged. Pin values are the deliberately ratified behavioral change, not compression.

The grant checker enforces the existing native grants; no grant is added or removed. A new field must now be reviewed rather than silently ignored. This strengthens the source gate, not a claim of runtime permission enforcement.
