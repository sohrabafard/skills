# Lane boundaries and limitations

Source: 22 Claude orchestrator role files plus the rule-writer are projected. The independent preservation check compares their complete bodies and non-pin metadata with HEAD. VERSION is 4.1.0. No Codex behavioral input changed, so no Codex generation or manifest refresh was required.

Packaging: the lead supplied a bounded lookup of scripts, runtime configuration directories and installation docs; no authoritative plugin producer or manifest was found. The source pack has no such producer. No build, package, install, runtime call or evaluation is claimed. Do not fabricate a build command from installation prose.

Installed visibility: native file metadata reports both Claude and Codex alaa-cc-orchestrator skill paths as SymbolicLinks into this repository. Source edits are therefore visible through those existing links. No link, installed user-agent copy, CLI, global configuration, commit or staged state was written by this lane.

A local preservation helper initially used Windows default text encoding and failed on a character decode. The cause-specific repair rebuilt only this lane's projected files from explicit UTF-8 HEAD text plus the intended pin edits. A subsequent exact body/non-pin comparison passed for all 23 files. This helper error did not change role behavior in the final candidate.

Compression: see instruction-draft.md and compression-evidence.md in this directory. No authority, output, grant, role-body or independent-gate instruction changed. The new gate rejects unapproved native grants and metadata; it does not prove runtime enforcement.
