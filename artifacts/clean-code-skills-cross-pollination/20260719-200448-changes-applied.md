# Changes applied — clean-code skills cross-pollination (2026-07-19)

All three phases executed. Companion doc: `20260719-195356-gap-matrix-and-plan.md` (full gap matrix).

## Phase 1 — cross-pollination

**alaa-php-clean-code** (from Vue + Go):
- `SKILL.md`: new "Size and complexity budgets" hard-gate section (class ≤400, method ≤60, one primary class/file, split seams); non-negotiables extended with presence-vs-truthiness merges, `env()` only in config + vocabulary-as-constants, `NEEDS_<X>_CONFIRMATION` marking; Laravel defaults gained declared route posture; error baseline gained failure classification (definitive vs transient) + outbound-HTTP hygiene; new "Validation before done" section (Pint/PHPStan/Pest commands + run-twice test); pattern decision order gained state-machine step.
- `references/laravel-best-practices.md`: PATCH presence semantics subsection; idempotency-as-contract + run-twice proof + outbound HTTP hygiene under jobs; Laravel 13 model-config attributes policy line; testing gained run-twice and two-tenant-sequence bullets.
- `references/php-modern-and-psr.md`: PHP 8.4 lazy objects (ghost/proxy) under modern features; truthiness-merge anti-pattern under type safety.

**alaa-vue-typescript-clean-code** (from PHP + Go):
- `SKILL.md`: public-contract inventory before refactors; double-fire-safe mutations invariant; boundary naming alignment gate; route-meta posture + unbounded-list rules in Quasar section; pattern list extended (see Phase 3).
- `references/20-typescript-composition-contract.md`: exhaustiveness (`assertNever`); vocabulary `as const` registries; time discipline (UTC/ISO at boundaries, immutable dates); double-fire safety + failure classification under async discipline; new "Version gates" block (Vue 3.5 APIs, Vue 3.6/Vapor experimental, Pinia 3, TS 6/7).
- `references/30-clean-code-solid-vue.md`: "Boundary naming alignment" section (artifact-family naming, no utils/common dumps).
- `references/50-quasar-vite-pinia-contract.md`: route-meta posture + ordered guard chain; "Large lists" section (server-side pagination / QVirtualScroll); Pinia 3 note.
- `references/60-validation-checklists.md`: contract-inventory + double-fire items in refactor checklist.
- `references/65-alaa-observed-patterns.md`: untouched (project-proven, locked).

**alaa-golang-clean-code-principles** (from Vue):
- P8 extended in `references/20-domain-data-and-consistency.md` (with wrong/right example) and `references/full-guide.md` (corollary): PATCH presence via pointers, zero-guard = truthiness-merge twin. No renumbering; boundary respected (size budgets/naming NOT ported — owned by golang-code-style/golang-naming).

## Phase 2 — web-refreshed facts folded in

- PHP 8.5 (2025-11-20) feature set re-verified — skill was already accurate.
- PHP 8.4 lazy objects (`newLazyGhost`/`newLazyProxy`) → PHP Proxy pattern + modern-features.
- Laravel 13 (2026-03-17): model-config PHP attributes policy line added; AI SDK/JSON:API opt-in stance already present, kept.
- Pinia 3: Vue-3-only, `defineStore({id})` removed → Vue references.
- TypeScript 6.0 (strict defaults flipped) / 7.0 (native Go compiler, 2026-07) → version gates.
- Vue 3.6 beta: alien-signals rewrite internal; Vapor opt-in experimental → version gates.

## Phase 3 — 15 design patterns (screenshot list)

- **Vue** `references/40-patterns-vue-quasar.md`: added Builder, Composite, Iterator, Template Method (composition-only framing), Chain of Responsibility; FSM section retitled "State (finite state machine)"; Decorator gained cache-decorator discipline. All 15 now covered.
- **PHP** `references/design-patterns.md`: added Builder, Proxy (PHP 8.4 lazy objects), Composite, Iterator (generators/LazyCollection), State (enum + match transition authority), Template Method (cautious), Chain of Responsibility/Pipeline linkage; Facade section now distinguishes GoF facade vs Laravel Facade static proxies; Contents realigned to actual section order (fixed a pre-existing mismatch). All 15 covered.
- **Go** new `references/60-design-patterns-kit-era.md`: 15-pattern decision map (pattern → kit-era shape → governing principle), routing mechanics to `golang-design-patterns` per the boundary contract; wired into `SKILL.md` navigation, `00-topic-map.md`, and annotated in `50-skill-boundaries.md`.

## Validation

- Header-order and content checks via grep/wc on edited files; PHP Contents list now matches physical section order; Vue pattern file covers all 15; Go map file 149 lines, boundary-consistent.
- No behavioral code to test — markdown skill pack only. Skill frontmatter untouched except none (names/descriptions unchanged).

## Open follow-ups

- Go split files 10/30/40 unchanged (P8 lives in 20 + full-guide; both updated in sync).
- `agents/openai.yaml` files not touched — descriptions there may eventually mention the new pattern coverage.
- Vue skill description in frontmatter still accurate; consider mentioning "15-pattern catalog" on a future description refresh.
