# Workflow Plan - Code intelligence routing skill upgrade

- Task ID: `20260801-172256_code-intelligence-routing-skill-upgrade`
- Mode: `plan`
- Profile: `resumable`
- Status: completed
- Created: `2026-08-01T17:22:56Z`
- Parent plan: not created
- Prompt pack: not created
- Checkpoint: `docs/agents/20260801-172256_code-intelligence-routing-skill-upgrade-state.md`
- Machine state: not created

## Summary and Outcome

- Initial repository truth: `alaa-code-intelligence-routing` was a seven-reference, script-free skill whose body assigned broad owner categories but did not provide a fully decidable joint CodeGraph/Serena/Laravel Boost sequence or complete project-binding templates.
- Achieved outcome: the skill is now an eight-reference, precise cross-runtime evidence-routing control system with source-backed Laravel, non-Laravel, and document-heavy bindings plus safe Serena configuration profiles.
- Strategy: Ground all capability and hook claims in live repository files and current primary sources; reconcile a single-owner routing state machine; implement through one bounded writer lane; then run independent verification and review.

## Scope

- In scope: `skills/sohrab/alaa-code-intelligence-routing/**`, required index entries if the pack contract demands them, this plan/checkpoint pair, and final copy-ready project-binding/configuration snippets.
- Out of scope: application code, vendor files, global MCP or hook configuration, repository-wide skill rewrites, commits, publishing, deployment, and changes to user projects outside this working tree.
- Constraints and assumptions: English-only repository artifacts; no machine-specific paths or model names in stable skill text; each rule has one owner; CodeGraph evidence is consumed unless a named gap remains; native repository checks are final proof; all version-sensitive claims require current authoritative sources.
- Control-layer boundary: the user-level `CODEGRAPH_START` block owns the baseline "CodeGraph before grep/find/file reads when locating or understanding supported code in an indexed repository" rule. Project bindings must not duplicate that procedure; they declare availability and skill invocation. This skill owns question classification, exceptions, named secondary gaps, degraded operation, and the return to native proof.
- Composition boundary: two questions are independent only when neither answer can change the other question's owner, query, authorization, or scope. Only such named questions may run in parallel. Discovery -> exact semantic work -> proof is a sequential change of question, not duplicate retrieval.
- Freshness boundary: an explicit CodeGraph stale/pending signal immediately routes only the named affected file(s) to a live read; a narrower graph retry cannot prove freshness. An unavailable or empty owner gets at most one health attempt. When fallback cannot preserve the original guarantee, especially broad flow or impact completeness, report partial/blocked rather than presenting it as equivalent.
- Hook ownership: this skill owns routing-hook topology (compatible CodeGraph prompt injection, Serena activation/cleanup, deliberate reminder omission, and the authorization caveat around optional auto-approval). Current official runtime documentation and installed CodeGraph/Serena commands own exact hook schema, locations, event support, trust, command shapes, and client compatibility. Copy-ready current snippets belong in the user handoff, not timeless skill doctrine.
- Safety and rollout boundary: CodeGraph root, Serena active project, Git diff, and native proof must identify the same worktree. Every Laravel Boost operation is classified by effect before execution. Adoption inspects installed command help/health, parses or validates configuration against the current schema, and exercises hooks in a disposable or ordinary session while observing exit/status behavior; failure degrades to a named fallback. Rollback removes only the project-local binding/hook/config delta and never implies reindexing, global mutation, or data cleanup.

## Handoff Package

Knowledge that lives only in the current agent's head and disappears on compaction. Fill a field when something is learned, not on a schedule; leave a field empty rather than padding it. See `references/context-continuity.md`.

- Confirmed facts (verified, each with how it was verified): the repository is CodeGraph-indexed (`Test-Path .codegraph`); the upgraded skill has exactly eight references and no scripts (bounded recursive inventory and independent verification); the orchestrator sentinel and pack version both equal `3.2.0` (direct file reads); the initial worktree was clean (`git status --short`); current installed/runtime evidence reports CodeGraph `1.5.0`, Serena `1.6.1`, and Codex CLI `0.146.0` (local CLI help/version checks); current Serena project configuration and official configuration guidance use `languages`, not `language_servers`; official capability pages establish CodeGraph as a supported-source relationship index, Serena as a symbol/semantic language-server surface, and Laravel Boost as Laravel-aware documentation/application context; current Codex hooks documentation includes `SessionStart` and `SessionEnd`; local `codegraph prompt-hook --help` identifies that hook as Claude `UserPromptSubmit` input.
- Open assumptions (believed but unverified, each with what would verify it): the exact Laravel Boost tool inventory varies by the package version installed in each consumer repository; verify the installed Boost package/server before relying on an individual tool name. Serena and client-hook schemas can drift; regenerate or compare each consumer's installed template before adoption.
- Ruled out (approach, reason, evidence): universal CodeGraph -> Serena -> Laravel Boost sequencing, because the current question determines the primary owner and a second owner is justified only by one named evidence gap; treating CodeGraph as the structural owner of Markdown, YAML, Dockerfile, Compose, Helm, or GitLab CI, because its supported-language list does not establish those artifact semantics; replacing Serena `languages` with `language_servers`, because both installed Serena `1.6.1` configuration and current official documentation use `languages`; adding `serena-hooks remind` beside CodeGraph's prompt hook, because it would create two competing routing governors; treating any installed Laravel Boost database, code-execution, runtime, data, secret, or production operation as implicitly authorized, because tool availability does not grant authority.
- Read first on resume (ordered exact paths): `docs/_agent_plans/20260801-172256_code-intelligence-routing-skill-upgrade.md`; `docs/agents/20260801-172256_code-intelligence-routing-skill-upgrade-state.md`; `skills/sohrab/alaa-code-intelligence-routing/SKILL.md`; `skills/sohrab/alaa-code-intelligence-routing/references/10-routing-contract.md`; `skills/sohrab/alaa-code-intelligence-routing/references/50-hooks.md`.
- Environment notes (command shapes that work here, and ones that look right but fail): `python skills\\sohrab\\alaa-workflow\\scripts\\init_workflow_files.py --task "Code intelligence routing skill upgrade" --profile resumable` created this artifact family successfully.
- Traps (looks correct, is not): CodeGraph does not establish current Markdown/config content merely because the repository is indexed; its first query returned unrelated Python symbols, so non-symbolic skill text still requires bounded native reads. "Use all available systems" is not a routing algorithm: parallel retrieval is valid only for independent named questions, while one current question has one primary evidence owner. Serena activation is not authorization to run onboarding, write memory, or mutate source/data. YAML language support does not supply Docker Compose, Helm, or GitLab CI domain semantics.

## Ordered Work

### Phase 1 - Ground requirements and design

- Status: completed
- Depends on: none
- Owned scope: read-only repository evidence, current primary-source research, acceptance contract, and routing architecture.
- Excluded from this phase: edits to the target skill and configuration outside this worktree.
- Work:
  - [x] Read the target skill, every bundled reference, pack routing map, and applicable owning-skill references.
  - [x] Verify CodeGraph, Serena, Laravel Boost, and hook claims from primary sources.
  - [x] Reconcile acceptance criteria, owner matrix, fallback gaps, and stack-specific bindings.
- Acceptance criteria: every proposed routing rule has an observable trigger, one primary owner, a named fallback condition, and a stop condition; version-sensitive claims have source-map entries.
- Validation commands: source URL checks and bounded repository cross-reference searches.
- Evidence observed: all target and directly applicable owning-skill files were read in full; official CodeGraph, Serena, Laravel Boost, Claude Code, and Codex hook documentation plus installed CLI/config behavior were checked. The baseline pack validator reports one target error (missing `When NOT to use`); the index checker reports the target missing from both pack indexes; the fleet-reference checker passes. The proposed state machine uses one question-scoped owner, consumes its evidence, opens at most one secondary owner for one named gap, and returns proof to repository-native commands.

### Phase 2 - Implement the routing contract

- Status: completed
- Depends on: Phase 1
- Owned scope: `skills/sohrab/alaa-code-intelligence-routing/**` and directly required pack index entries only.
- Excluded from this phase: vendor files, unrelated skills, global settings, and user application repositories.
- Work:
  - [x] Rewrite the decision procedure and references without duplicating owner doctrine.
  - [x] Add copy-ready bindings/config templates in the owning hook/binding reference when justified.
- Acceptance criteria: the skill decides solo versus combined owner use for source, config, docs, Laravel runtime/context, edits, handoff, freshness failures, and validation; all references resolve; the description stays within pack limits.
- Validation commands: `python scripts\\validate_sohrab_skill_pack.py`; `python scripts\\check_skill_index.py`; `python scripts\\check_fleet_references.py`.
- Evidence observed: the writer updated the skill body, metadata, all seven existing references, and added `references/70-project-bindings.md`, bringing the skill to eight references while retaining the required body router. The new reference contains placeholder-free Laravel, non-Laravel, and document-heavy bindings plus `languages:` profiles and a common `initial_prompt`. The documentation lane added the missing target entry to both pack indexes and reconciled the live 68/68 metadata count. Writer gates passed: skill-pack validator, fleet-reference checker, and scoped diff check. Index-lane gates passed: index checker and fleet-reference checker.

### Phase 3 - Independent verification and review

- Status: completed
- Depends on: Phase 2
- Owned scope: complete integrated diff and declared validation surface.
- Excluded from this phase: feature expansion unrelated to findings.
- Work:
  - [x] Run deterministic validators through an independent verifier.
  - [x] Run a fresh-context independent review; route blocker/major findings to the writer for at most two fix cycles.
  - [x] Re-run affected and integrated gates after fixes.
- Acceptance criteria: mandatory validators pass, no unresolved blocker/major finding remains, and the final diff matches declared scope.
- Validation commands: target skill validator surface above plus `git diff --check` and workflow semantic validation.
- Evidence observed: initial integrated verification passed all six dispatched gates and parsed the first four YAML fences. Fresh-context review returned three majors and three minors covering prompt-injection topology, hook ownership, language-backend installation authority, proof wording, version-sensitive Boost inventory, and unsupported dry-run attribution. Fix cycle 1 resolved all six. Post-fix verification passed all six gates, parsed all seven YAML fences, confirmed eight references and the body router, and found no contamination. The final closure reviewer returned `APPROVED` with no findings after two workflow/index consistency corrections.

### Phase 4 - Reconcile deliverables and close

- Status: completed
- Depends on: Phase 3
- Owned scope: workflow artifacts and user-facing copy-ready bindings.
- Excluded from this phase: external configuration writes or commits.
- Work:
  - [x] Synchronize plan/checkpoint with observed evidence.
  - [x] Report optimized Laravel, non-Laravel, and document-heavy bindings plus final Serena `initial_prompt` and language guidance.
- Acceptance criteria: repository artifacts, validation evidence, and final recommendations agree; all unverified limits are explicit.
- Validation commands: `python skills\\sohrab\\alaa-workflow\\scripts\\validate_workflow_files.py --plan docs\\_agent_plans\\20260801-172256_code-intelligence-routing-skill-upgrade.md`; final `git status --short` and scoped diff inspection.
- Evidence observed: the final bindings, minimal/optional language profiles, and common `initial_prompt` are stored in `references/70-project-bindings.md`. Current Claude and Codex handoff JSON was parsed successfully with `ConvertFrom-Json`. Final repository gates returned exit 0 for the pack validator, index checker, fleet-reference checker, workflow validator, and `git diff --check`; exact current snippets and adoption caveats are included in the user handoff.

## Delegation

- Keep shared-context work in the main conversation.
- Independent lane ownership: specification analyst owns the acceptance contract; researcher owns current official capability/hook evidence; architecture critic owns the routing-state pressure test; one implementer owns the target skill directory; verifier and reviewer are read-only gates.
- Dispatches assume zero shared context: copy the relevant handoff-package facts into the dispatch text rather than referring to this conversation.

## Blockers and Next Action

- Blockers: none.
- Next action: user handoff only; no commit, installation, or external configuration write was authorized.
