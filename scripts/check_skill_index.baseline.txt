# Baseline for check_skill_index.py.
# One finding per line: <rule-id>|<subject>|<location>|<item>
# The line number is deliberately absent from the key, so an unrelated edit above the subject
# does not churn this file. A baseline entry that matches no finding exits 1, so this list can
# only shrink: each entry expires by itself the moment the missing file appears, and none of
# them can become a permanent amnesty.
#
# This file is currently empty of entries, and that is the intended end state. It shipped on
# 2026-07-30 with two X8-MISSING-OPENAI-YAML entries, for alaa-cc-orchestrator and
# alaa-go-chi-development. Both files were written on 2026-07-31, so both entries expired as
# designed and were removed. Keep the file rather than deleting it: --baseline is named in the
# documented invocation, and a missing path is a "could not run" the checker reports as exit 2.
