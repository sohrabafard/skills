---
name: marker-skill
---
# Marker skill

Bundled, marked and checked: run `$SKILL_DIR/scripts/tool.py --self-test`.

In the target repository, write `<repo>/docs/BIG_PICTURE.md` and `<repo>/charts/gateway/templates/configmap.yaml`.

Not yet marked, so reported as informational and never as a failure: write the summary into
`docs/api-summary.md` and the CI job into `ci/scripts/deploy.sh`.

## When NOT to use
Never.
