interface:
  display_name: Fixture Alpha
  short_description: "Validator fixture skill, alpha"
  default_prompt: "Use $alaa-services-contract only inside the validator self-test."
  brand_color: "#3A5BA0"
policy:
  allow_implicit_invocation: false
