# Orchestrator

## 5. Pipeline

Six phases, in order.

### Final report

Report in this order: the outcome, then the completion-lifecycle states — `IMPLEMENTED`, `MERGE_CANDIDATE`, `RELEASE_CANDIDATE` — each carrying its own verdict, with `alaa-workflow references/workspace-and-integration.md` owning what earns each state; then changes by lane.

## 6. Advisor-mode output

Provide grounded repository findings.
