@AGENTS.md

This file is an import bridge and holds no rules of its own. The line above imports `AGENTS.md`,
which is the single source of truth for both runtimes; edit that file, not this one. A rule that is
true only under Claude Code, and under no other runtime, is the one thing that may be added below.
