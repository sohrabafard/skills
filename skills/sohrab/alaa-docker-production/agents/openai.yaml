interface:
  display_name: "Alaa Docker Production"
  short_description: "Production Docker hardening for Laravel"
  default_prompt: "Use $alaa-docker-production to follow its workflow for production docker hardening for laravel."

policy:
  allow_implicit_invocation: true
