interface:
  display_name: "Alaa HAProxy"
  short_description: "HAProxy config, ops, and observability"
  default_prompt: "Use $alaa-haproxy to follow its workflow for HAProxy config, production operations, and upgrade guidance."

policy:
  allow_implicit_invocation: true
