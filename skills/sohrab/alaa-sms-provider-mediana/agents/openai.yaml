interface:
  display_name: "Alaa SMS Provider Mediana"
  short_description: "Mediana/IPPanel SMS provider contract"
  default_prompt: "Use $alaa-sms-provider-mediana to implement, review, debug, or test Alaa SMS channel integrations with Mediana/IPPanel Edge API using env-configured endpoints, Authorization auth, correct send variants, phone normalization, response parsing, and safe validation."

policy:
  allow_implicit_invocation: true
