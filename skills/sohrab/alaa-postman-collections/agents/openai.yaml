interface:
  display_name: "Alaa Postman Collections"
  short_description: "Postman and SDK-ready API contract sync"
  default_prompt: "Use $alaa-postman-collections to synchronize the Postman artifacts and SDK-ready public API contract, then validate complete request and response coverage plus Insomnia portability."

policy:
  allow_implicit_invocation: true
