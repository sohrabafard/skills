interface:
  display_name: "Alaa Partitioned Table FK Audit"
  short_description: "Postgres partitioned-table FK bug audit, with a shipped detector"
  default_prompt: "Use $alaa-partitioned-table-fk-audit to run scripts/partitioned_fk_audit.py over this repository's schema roots, fix any incomplete key referencing a partitioned parent, and install the detector as a durable regression test."

policy:
  allow_implicit_invocation: true
