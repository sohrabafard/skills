---
# Fixture: six planted secrets, one per shape. scan_secrets.sh --self-test
# asserts that all six are reported by name. Do not fix this file.
#
# 1 hardcoded password       2 AWS access key ID     3 AWS secret access key
# 4 API token                5 database DSN          6 private key block
- name: Fixture play holding six planted credentials
  hosts: localhost
  gather_facts: false

  vars:
    db_password: "hunter2-plaintext"
    aws_access_key_id: AKIAIOSFODNN7EXAMPLE
    aws_secret_access_key: "wJalrXUtnFEMIK7MDENGbPxRfiCYEXAMPLEKEY"
    api_token: "ghp_0123456789abcdefghijklmnopqrstuvwxyzAB"
    dsn: "postgres://svc:s3cr3tpass@db.internal:5432/app"
    deploy_key: |
      -----BEGIN RSA PRIVATE KEY-----
      MIIEowIBAAKCAQEAy8Dbv8prpJ/0kKhlGeJYozo2t60EG8L0561g13R29LvMR5hy
      -----END RSA PRIVATE KEY-----

  tasks:
    - name: Reference the planted values so they are not dead configuration
      ansible.builtin.debug:
        msg: "fixture"
