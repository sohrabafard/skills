# Example `.codex/config.toml` for this skill

[features]
multi_agent = true

[agents.core]
description = "Implement the Shaka core wrapper, lifecycle, networking, and stats."
config_file = "agents/core.toml"

[agents.ads]
description = "Implement VAST and VMAP ad orchestration with IMA and failure recovery."
config_file = "agents/ads.toml"

[agents.analytics]
description = "Implement watch-time analytics, user interactions, QoE, and error telemetry."
config_file = "agents/analytics.toml"

[agents.overlay]
description = "Implement quiz overlays and timeline marker hooks for the UI layer."
config_file = "agents/overlay.toml"

[agents.conductor]
description = "Implement TV-like schedule playback and playlist fallback."
config_file = "agents/conductor.toml"

[agents.qa]
description = "Review lifecycle safety, browser behavior, edge cases, and regression risks."
config_file = "agents/qa.toml"
