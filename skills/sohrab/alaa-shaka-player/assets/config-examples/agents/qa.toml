model = "gpt-5.5"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"
developer_instructions = """
Act as a QA and review agent. Focus on browser matrix coverage, lifecycle leaks,
regressions, edge cases, and missing test scenarios.
"""
