interface:
  display_name: "Alaa Low Noise"
  short_description: "Context-safe Codex execution with compact output and diff-preserving repo-local state"
  brand_color: "#0f766e"
  default_prompt: "Use $alaa-low-noise to keep Codex CLI/App work complete but low-noise: search first, read bounded excerpts, summarize instead of dumping, externalize bulky transient state to repo-local files when useful, and preserve normal Git diffs and Codex app review visibility."

policy:
  allow_implicit_invocation: false