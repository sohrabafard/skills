interface:
  display_name: "Alaa Golang"
  short_description: "Go router for code, Fiber, and delivery"
  default_prompt: "Use $alaa-golang to route this Go task through the right Go, Fiber, and Sohrab companion skills."

policy:
  allow_implicit_invocation: true
