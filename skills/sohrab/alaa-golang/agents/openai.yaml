interface:
  display_name: "Alaa Golang"
  short_description: "Route production Go work and service patterns"
  default_prompt: "Use $alaa-golang to route this Go task, choose chi or Fiber, enforce repository pattern, Redis cache safety, TDD, and production readiness."

policy:
  allow_implicit_invocation: true
