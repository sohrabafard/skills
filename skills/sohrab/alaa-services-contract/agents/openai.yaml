interface:
  display_name: "Alaa Services Contract"
  short_description: "Ala service, gateway, role, runtime, and API contract"
  default_prompt: "Use $alaa-services-contract to align this service with the shared Ala API, gateway prefix map, compact role projection, auth terms/TOTP policy, trust boundary, permission catalog, notification messaging, observability, runtime, and deployment contracts."

policy:
  allow_implicit_invocation: true
