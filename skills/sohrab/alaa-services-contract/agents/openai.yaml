interface:
  display_name: "Alaa Services Contract"
  short_description: "Hard Ala service, gateway prefix-map, runtime, and permission contract"
  default_prompt: "Use $alaa-services-contract to align this service with the shared Ala API, gateway prefix-map, trust-boundary, permission-catalog, observability, runtime, and deployment contract."

policy:
  allow_implicit_invocation: true
