interface:
  display_name: "Alaa Services Contract"
  short_description: "Hard Ala backend, gateway-facing, and deploy contract"
  default_prompt: "Use $alaa-services-contract to enforce the Ala backend service contract for `/api/health`, `/api/ready`, trusted ingress, Laravel API responses, frontend-to-gateway-to-backend flow, backend behavior behind the Ala gateway, Arvan-versus-Docker deploy modes, canonical Docker DNS aliases, shared infra, mandatory pull-through public image registry usage, and registry or key-handling rules."

policy:
  allow_implicit_invocation: true
