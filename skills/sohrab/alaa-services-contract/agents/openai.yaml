interface:
  display_name: "Alaa Services Contract"
  short_description: "Hard Ala backend contract for routes, headers, signals, and Laravel API responses"
  default_prompt: "Use $alaa-services-contract to enforce the exact Ala backend contract for `/api/health`, `/api/ready`, service naming, RequestObservabilityMiddleware, ResolveUserMiddleware, event/code naming, and Laravel Resource-first `/api/*` responses."

policy:
  allow_implicit_invocation: true
