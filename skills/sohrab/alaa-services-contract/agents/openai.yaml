interface:
  display_name: "Alaa Services Contract"
  short_description: "Cross-service contracts, readiness, and Laravel response rules"
  default_prompt: "Use $alaa-services-contract to apply the hardened Ala backend service contract, readiness rules, and Laravel-only API response rules when relevant."

policy:
  allow_implicit_invocation: true
