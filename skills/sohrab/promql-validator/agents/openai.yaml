interface:
  display_name: "Promql Validator"
  short_description: "Validate PromQL queries"
  default_prompt: "Use $promql-validator to follow its workflow for validate promql queries."

policy:
  allow_implicit_invocation: false
