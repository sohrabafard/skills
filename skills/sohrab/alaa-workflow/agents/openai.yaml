interface:
  display_name: "Alaa Workflow"
  short_description: "Phase-based workflow for long tasks"
  default_prompt: "Use $alaa-workflow to follow its workflow for phase-based workflow for long tasks."

policy:
  allow_implicit_invocation: false
