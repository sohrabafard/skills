# Source Map

Use this file when the task depends on current package state, service adoption state, or release/version behavior.

## Local source priority

1. Active target repository code and tests.
2. `D:\Sohrab\Project\alaa-controlled-ops` docs and package code.
3. Consuming service docs, Postman artifacts, and execution state files.
4. Composer lock files and Satis/dist metadata.
5. Historical session evidence only as a search hint.

## Companion skills

- `$alaa-services-contract` for platform-wide service contracts and public API shape.
- `$alaa-trust-gateway-auth` for trusted gateway context, headers, permission bitmaps, and TOTP proof propagation.
- `$alaa-postman-collections` for Postman collection and environment sync.
- `$alaa-docs-farsi` for README, BIG_PICTURE, API summary, data, and observability docs.
- `$alaa-php-clean-code` for Laravel and PHP code quality boundaries.
- `$alaa-data-layer` for persistence, transactions, and Postgres truth.
- `$service-runtime-kit-governance` for local runtime generation and Docker wrapper issues.

## Freshness triggers

Refresh from source before answering when the task mentions latest package version, current route count, current Postman count, permission ID allocation, Satis tag visibility, Composer lock source, or a specific ControlledOps phase.
