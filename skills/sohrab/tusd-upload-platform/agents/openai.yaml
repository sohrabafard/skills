interface:
  display_name: "Tusd Upload Platform"
  short_description: "Secure tusd platform design and client flows"
  default_prompt: "Use $tusd-upload-platform to follow its workflow for secure tusd platform design and client flows."

policy:
  allow_implicit_invocation: true
