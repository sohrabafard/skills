interface:
  display_name: "Terraform Validator"
  short_description: "Validate, lint, security-scan, and dry-run Terraform/OpenTofu"
  default_prompt: "Use $terraform-validator to run fmt/init/validate, Checkov security scan, and plan on Terraform/OpenTofu, look up provider docs, and loop fix-and-revalidate until clean. Read its references for the full workflow."

policy:
  allow_implicit_invocation: true
