interface:
  display_name: "Terraform Validator"
  short_description: "Validate Terraform IaC"
  default_prompt: "Use $terraform-validator to follow its workflow for validate terraform iac."

policy:
  allow_implicit_invocation: false
