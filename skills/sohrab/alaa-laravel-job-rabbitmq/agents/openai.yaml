interface:
  display_name: "Alaa Laravel Job Rabbitmq"
  short_description: "Laravel RabbitMQ worker design"
  default_prompt: "Use $alaa-laravel-job-rabbitmq to follow its workflow for laravel rabbitmq worker design."

policy:
  allow_implicit_invocation: true
