interface:
  display_name: "Alaa Trust Gateway Auth"
  short_description: "Gateway trust boundary, X-Access, step-up, fail-closed"
  default_prompt: "Use $alaa-trust-gateway-auth for who may assert a trusted request header behind the Ala gateway: sanitize and injection symmetry, compact JWT claim projection, X-Access permission-bitmap consumption, TOTP step-up headers, tenant scoping, auth-service v3 route shape, and every fail-closed case at that boundary."

policy:
  allow_implicit_invocation: true
