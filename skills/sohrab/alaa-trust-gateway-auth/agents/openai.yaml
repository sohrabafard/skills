interface:
  display_name: "Alaa Trust Gateway Auth"
  short_description: "Gateway trust, JWT roles, X-Access, and auth context"
  default_prompt: "Use $alaa-trust-gateway-auth to follow its workflow for Ala gateway trust, JWT claim projection including rol to X-User-Roles, X-Access permission bitmap semantics, request-time authorization, frontend route expectations, and downstream auth context."

policy:
  allow_implicit_invocation: true
