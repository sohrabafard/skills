
# Install this skill

Recommended locations:
- Repo-local: `<repo>/.agents/skills/clickhouse-performance-schema-ops/`
- User-level: `~/.agents/skills/clickhouse-performance-schema-ops/`

After copying:
1. restart Codex if the skill does not appear
2. invoke explicitly via `/skills` or `$clickhouse-performance-schema-ops`

This skill is explicit-first (`allow_implicit_invocation: false`) to avoid accidental activation for generic SQL tasks.
