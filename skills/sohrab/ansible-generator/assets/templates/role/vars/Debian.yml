---
# OS-specific variables for the Debian family, loaded by tasks/main.yml through
# include_vars on ansible_os_family.

ROLE_NAME_package_name: "CHANGE_ME_debian_package"
ROLE_NAME_service_name: "CHANGE_ME_debian_service"
ROLE_NAME_config_dir: "/etc/CHANGE_ME_debian_service"
