---
# OS-specific variables for the RedHat family, loaded by tasks/main.yml through
# include_vars on ansible_os_family.

demo_nginx_package_name: "nginx"
demo_nginx_service_name: "nginx"
demo_nginx_config_dir: "/etc/nginx"
