---
# Role: demo_nginx
# Role metadata.

galaxy_info:
  role_name: demo_nginx
  namespace: fixture
  author: ansible-generator fixtures
  description: Install and configure nginx, as a scaffold fixture
  company: none
  license: MIT
  min_ansible_version: "2.19"

  # Only list a platform this role was actually tested on. A platform in this
  # list that nobody tested is a claim the role cannot support.
  platforms:
    - name: Ubuntu
      versions:
        - jammy
        - noble
    - name: Debian
      versions:
        - bookworm
        - trixie
    - name: EL
      versions:
        - "9"
        - "10"

  galaxy_tags:
    - web
    - nginx

dependencies: []
