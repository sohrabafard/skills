interface:
  display_name: "Vector Observability Pipelines"
  short_description: "Vector pipelines, buffers, VRL, ClickHouse sink"
  default_prompt: "Use $vector-rust-observability-pipelines for Vector topology, VRL transforms, buffering and acknowledgement choices, ClickHouse sink tuning, and what the pipeline does when its destination is unreachable."
  brand_color: "#5B4B9F"
policy:
  allow_implicit_invocation: true
