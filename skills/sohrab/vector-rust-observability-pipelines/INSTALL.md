
# Install this skill

Recommended locations:
- Repo-local: `<repo>/.agents/skills/vector-rust-observability-pipelines/`
- User-level: `~/.agents/skills/vector-rust-observability-pipelines/`

After copying:
1. restart Codex if needed
2. invoke via `/skills` or `$vector-rust-observability-pipelines`

This skill is explicit-first (`allow_implicit_invocation: false`) to avoid accidental activation for generic logging questions.
