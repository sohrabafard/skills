# Example Note

Base document of a valid localized pair. Every link below must resolve.

## Setup

See [the generics page](./generics.md) and its [inline code section](./generics.md#inline-code).

## Setup

A duplicate heading, so [the second one](#setup-1) must resolve.

```text
docs/api-summary.md
```
