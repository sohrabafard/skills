interface:
  display_name: "Helm Generator"
  short_description: "Generate Helm charts"
  default_prompt: "Use $helm-generator to follow its workflow for generate helm charts."

policy:
  allow_implicit_invocation: false
