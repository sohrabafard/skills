interface:
  display_name: "Alaa Data Layer"
  short_description: "Store choice, Postgres schema, Redis cache policy"
  default_prompt: "Use $alaa-data-layer for store selection, Postgres schema and query work, and Redis cache, lock, or degraded-mode design."

policy:
  allow_implicit_invocation: true
